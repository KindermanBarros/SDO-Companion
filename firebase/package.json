{
  "name": "sdo-companion-firebase-rules",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "cd .. && firebase/node_modules/.bin/firebase emulators:exec --config firebase.json --only firestore --project sdo-companion-test \"node --test firebase/tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.18.0",
    "firebase-tools": "15.28.2"
  }
}
