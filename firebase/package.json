{
  "name": "sdo-companion-firebase-rules",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "cd .. && node firebase/node_modules/firebase-tools/lib/bin/firebase.js emulators:exec --config firebase.json --only firestore --project sdo-companion-test \"node --test firebase/tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.18.0",
    "firebase-tools": "15.28.2"
  }
}
